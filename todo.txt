Auto change donatecheck to 1 when others are selected
    Make if/else statement that will load on page load that says the following for every charity
        if hrc == 1, 
            return donatecheck == 1
        else
            return dontecheck == 0 
            
            
Database Management (easily find and add up values fromt he database per charity db query)