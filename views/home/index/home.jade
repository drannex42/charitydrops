extends homelayout

block content
    if !user
            section#intro
              .container
                    center
                        h1 Let's make the world better.
                        | <br><br>
                        a.button.btn.btn.btn-primary(href='/auth/facebook')
                                | Register with Facebook
                        | <br><br>
                        a(href='#learnmore')
                            | Learn more about how it works
            // Section 2
            section#learnmore
              .container
                        h3 Charities

    else 
        if user.user
        script.
            window.location = "/dashboard"

